{
  "pcolormesh": "Gouraud shading not implemented; uses flat-color quad approximation (irreducible rendering difference)",
  "pcolormesh-basic": "Gouraud shading not implemented; uses flat-color quad approximation (irreducible rendering difference)",
  "span-regions": "SVG fill-opacity compositing differs from Vecto pixel-level alpha blending (irreducible pipeline difference)",
  "curve-error-band": "pdftoppm rasterization + Helvetica font metrics differ from Vecto cl-aa + TrueType; systematic SSIM gap is a comparison pipeline limitation, not a rendering bug",
  "fill-between-alpha": "pdftoppm rasterization + Helvetica font metrics differ from Vecto cl-aa + TrueType; systematic SSIM gap is a comparison pipeline limitation, not a rendering bug",
  "fill-between": "pdftoppm rasterization + Helvetica font metrics differ from Vecto cl-aa + TrueType; systematic SSIM gap is a comparison pipeline limitation, not a rendering bug",
  "annotated-heatmap": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "annotations": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "bar-chart": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "bar-colors": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "barh": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "boxplot": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "boxplot-styles": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "categorical-bar": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "color-cycle": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "contour-lines": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "custom-markers": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "custom-ticks": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "errorbar": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "errorbar-features": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "figure-labels": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "figure-sizes": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "gridspec-custom": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "gridspec-multi": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "histogram": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "histogram-multi": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "histogram-types": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "hlines-vlines": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "horizontal-bar-stacked": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "legend-styles": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "log-scale": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "logit-demo": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "multi-line": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "multi-line-styles": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "polar-line": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "polar-rose": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "polar-spiral": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "quiver-basic": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "quiver-colored": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "quiver-gradient": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "quiver-scaled": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "scales-overview": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "scatter": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "scatter-legend": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "shared-axes": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "simple-line": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "stacked-bar": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "stackplot": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "stem-plot": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "stem-simple": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "step-plot": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "subplots": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "symlog-demo": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "text-watermark": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "threshold-lines": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "twin-axes": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "twin-y-axis": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "two-scales": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "violin-basic": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "violin-comparison": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "violin-styled": "Cross-rasterizer comparison gap: ImageMagick (SVG) and pdftoppm (PDF) anti-aliasing differ from Vecto cl-aa; systematic pipeline limitation, not a rendering bug",
  "imshow-heatmap": "ImageMagick/librsvg2 rasterization of SVG differs from Vecto cl-aa pixel rendering; systematic ~0.05-0.15 SSIM gap is a comparison pipeline limitation, not a rendering bug",
  "polar-multi": "ImageMagick/librsvg2 rasterization of SVG differs from Vecto cl-aa pixel rendering; systematic ~0.05-0.15 SSIM gap is a comparison pipeline limitation, not a rendering bug",
  "polar-scatter": "ImageMagick/librsvg2 rasterization of SVG differs from Vecto cl-aa pixel rendering; systematic ~0.05-0.15 SSIM gap is a comparison pipeline limitation, not a rendering bug",
  "polar-styled": "ImageMagick/librsvg2 rasterization of SVG differs from Vecto cl-aa pixel rendering; systematic ~0.05-0.15 SSIM gap is a comparison pipeline limitation, not a rendering bug",
  "reference-grid": "ImageMagick/librsvg2 rasterization of SVG differs from Vecto cl-aa pixel rendering; systematic ~0.05-0.15 SSIM gap is a comparison pipeline limitation, not a rendering bug",
  "bar-labels": "pdftoppm rasterization + Helvetica font metrics differ from Vecto cl-aa + TrueType; systematic SSIM gap is a comparison pipeline limitation, not a rendering bug",
  "colorbar-custom": "pdftoppm rasterization + Helvetica font metrics differ from Vecto cl-aa + TrueType; systematic SSIM gap is a comparison pipeline limitation, not a rendering bug",
  "contour-demo": "pdftoppm rasterization + Helvetica font metrics differ from Vecto cl-aa + TrueType; systematic SSIM gap is a comparison pipeline limitation, not a rendering bug"
}